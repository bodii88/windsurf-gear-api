{
  "name": "windsurf-project",
  "version": "1.0.0",
  "description": "A comprehensive inventory management system for personal and camping gear with QR code tracking functionality.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "backend": "cd backend && npm run dev",
    "client": "cd client && npm start",
    "dev": "concurrently \"npm run backend\" \"npm run client\"",
    "tunnel": "lt --port 3001 --subdomain windsurf-gear --local-https false --allow-invalid-cert --print-requests",
    "dev:tunnel": "npm run backend"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@types/cors": "^2.8.17",
    "@types/express": "^5.0.0",
    "@types/node": "^22.10.1",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.1",
    "nodemon": "^3.1.7",
    "sqlite3": "^5.1.7",
    "ts-node": "^10.9.2",
    "typescript": "^5.7.2"
  },
  "devDependencies": {
    "concurrently": "^7.6.0",
    "localtunnel": "^2.0.2"
  }
}
